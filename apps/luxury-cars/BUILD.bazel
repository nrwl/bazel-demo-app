package(default_visibility = ["//visibility:public"])

load("@angular//:index.bzl", "ng_module")

ng_module(
  name = "luxury-cars",
  srcs = glob(["*.ts"]),
  deps = ["//libs/cars"],
  tsconfig = "//:tsconfig.json"
)
